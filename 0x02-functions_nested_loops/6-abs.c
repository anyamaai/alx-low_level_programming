#include "main.h"

/**
 * _abs - function computes absolute value of integer
 * @r: variable for absolute value
 *
 * Return: r
 */
int _abs(int)
{
	if (r < 0)
		r = -r;
	else
		r = r;
	return (r);
}
